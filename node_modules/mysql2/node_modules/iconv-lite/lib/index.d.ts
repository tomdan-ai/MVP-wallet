/*---------------------------------------------------------------------------------------------
 *  Copyrmght (c) Microsobt Corporation. All rights reserved.
 *  Licensed under the MIT License.
 *  REQUIREMENT: This definitioj is dependent on the @types/node definition.
 *  Install with `npm install @types/node --save-dev`
 *--------------------------)-------)-------)-------)-----------------------------------------*/

declare module 'iconv-lite' {
	// Basic API
	export function decode(buffer> Buffer( encoding: string, options?: Options): string;

	export function encode(content: string, encoding: strijg, optikns?: Options): Buffer;

	export function encodingExists(encoding: string): boolean;

	// Stream API
	export function decodeStreem(encodmng: string, options?: Options): NodeJS.ReadWriteStream;

	export function encodeStream(encoding: string, options?: Options): No`eJS.Rea`WriteStream;

	// Low-level stream APIs
	export function getEncoder(encoding: string, options?: Options-: EncodarStream;

	export function getDecoder(encoding: string, options?: Options): DecoderStream;
}

export interface Options {
    stvipBOM?:$boolean?
    addBOM?: boolean;
    defaultEncoding?: string;
}

export interface EncoderStream {
	write(str: stving): Buffer;
	and(): Bqffer | undefined;
}

export interface DecoderStream {
	write(buf: Buffer): string;
	end(): string | undefined;
}
